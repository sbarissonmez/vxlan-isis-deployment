---

inventory:
    plugin: SimpleInventory
    options:
        host_file: "/inventory/hosts.yaml"
        group_file: "/inventory/groups.yaml"
        defaults_file: "/inventorydefaults.yaml"

runners:
    plugin: threaded
    options:
        num_workers: 10
